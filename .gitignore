# Hugo build output -- rebuilt inside the container, never committed.
public/
public_v2/
public_v3/
serve/

# Hugo caches.
resources/_gen/
.hugo_build.lock

# Theme git internals (theme is vendored, not a submodule).
themes/xmin/.git

# Editor / OS cruft.
*.swp
*.swo
.DS_Store

# Header screenshots from visual QA.
header-screenshot.png
.scan-commit-queue
